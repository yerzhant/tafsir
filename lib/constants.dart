import 'package:flutter/material.dart';

const server = 'https://azan.ru';

const primaryColor = Colors.indigoAccent;
// const primaryColor = const Color(0xff263b7c);

const textColor = const Color(0xFF414141);
const textColorGrey = Colors.grey;

const padding = 16.0;

const iconSize = 20.0;
